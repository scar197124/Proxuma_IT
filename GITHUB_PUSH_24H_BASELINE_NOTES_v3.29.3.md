# GitHub Push Notes — Proxuma IT v3.29.3 24H Baseline

Commit message suggestion:

```text
Lock Proxuma IT v3.29.3 RDAP fallback baseline
```

After pushing, let the live version breathe for 24 hours unless a visible break appears.

Quick live checks:

- Page loads at GitHub Pages.
- Scanner runs for a clean URL and a suspicious URL.
- Link Anatomy populates.
- Deep Analysis collapses and expands correctly.
- Case File stays scrollable internally.
- Online Intel does not run automatically.
- RDAP on GitHub Pages explains that `/api/proxuma-rdap` needs Vercel/serverless hosting.

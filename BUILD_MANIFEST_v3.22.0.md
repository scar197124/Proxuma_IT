# Build Manifest — Proxuma IT v3.22.0

Build: QR / Manual Payload UX Upgrade

Primary change: adds a clearer Manual Payload Helper so users can paste decoded QR text, copied message text, or raw payload content without relying on camera scanning.

Validation target: preserve offline-first behavior, local history, case packet export, mobile scan mode density, and footer-only PROXUMA return path.

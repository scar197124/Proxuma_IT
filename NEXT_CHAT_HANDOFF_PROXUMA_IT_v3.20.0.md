# Next Chat Handoff — Proxuma IT v3.20.0

Current baseline: Proxuma IT v3.20.0 — Case Packet Export.

This build adds `Download TXT` to Signal Evidence so users can save a readable local case report after a scan. It preserves the existing structured JSON case packet, the v3.19.10 mobile Scan Mode density polish, and the footer-only return path to canonical PROXUMA.

Recommended next steps: live-test TXT export on desktop and mobile, then consider a small README usage guide for reports before adding larger engine features.

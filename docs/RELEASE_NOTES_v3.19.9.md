# Proxuma IT v3.19.9 — Single Return Path Cleanup

This release removes duplicate return-to-canonical links from the header and hero section while preserving the footer `← PROXUMA Home` button as the single clean return path back to the canonical PROXUMA hub.

No scanner-engine changes were made.

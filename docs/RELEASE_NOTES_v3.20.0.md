# Proxuma IT v3.20.0 — Case Packet Export

This release adds a readable local TXT case report beside the existing structured JSON evidence packet.

## Added

- `Download TXT` inside Signal Evidence for readable case reports.
- TXT export includes verdict, input type, primary trigger, threat lane, target, user guidance, evidence, preservation checklist, and privacy boundary.
- The existing `Download JSON` case packet remains available for structured evidence and future import workflows.

## Preserved

- Offline-first behavior.
- No hidden online lookups.
- No telemetry.
- No active API/provider calls.
- v3.19.10 mobile Scan Mode button density polish.
- Footer-only return path to canonical PROXUMA.

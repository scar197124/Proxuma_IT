# Next Chat Handoff — Proxuma IT v3.23.1

Current baseline: Proxuma IT v3.23.1 — Link Anatomy Fill + Compact Guard.

This patch follows v3.23.0 and fixes the Link Anatomy card so it actually populates after scans. It also keeps the card compact to avoid UI clutter.

Preserve:
- Unified scanner input.
- Local scan history.
- TXT/JSON case export.
- Report view centered balance.
- Footer-only return path to canonical PROXUMA.
- Offline-first / no telemetry / no hidden online calls.

Watch next:
- Avoid adding more cards unless they replace/combine existing surfaces.
- Prefer compact rows, drawers, or existing sections over new visible blocks.
